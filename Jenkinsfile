pipeline {
    agent any
    stages {
        stage('Example Username/Password') {
            steps {
                sh 'echo "Service user is"'
                sh 'curl https://myservice.example.com'
            }
        }
    }
}
